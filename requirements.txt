# Python dependencies for the whole project: runtime, build backend, tests and
# documentation. CMake (>= 3.15), a C++17 compiler and Eigen3 are NOT Python
# packages and must be installed separately; see the README.

# Runtime
numpy>=1.20.0
numba>=0.55.0

# Build backend (compiles the C++ extensions through scikit-build-core)
scikit-build-core
pybind11

# Testing and benchmarking
pytest>=7.0.0
pytest-benchmark

# Documentation
sphinx>=5.0
pydata-sphinx-theme>=0.13
numpydoc
setuptools_scm
